#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name="TTD",
    version="0.0.1",
    description="TurnTaking Datasets",
    author="Erik",
    author_email="",
    install_requires=["torch"],
    packages=find_packages(),
)
